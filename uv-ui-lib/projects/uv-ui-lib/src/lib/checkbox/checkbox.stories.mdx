import { Meta, Story, ArgsTable, Source, Typeset, Preview } from '@storybook/addon-docs/blocks';
import { CheckboxComponent } from './checkbox.component';
import dedent from 'ts-dedent';
import * as stories from "./checkbox.stories.ts";

<Meta 
    title='Components/CheckboxMDX' 
    component={CheckboxComponent} 
/>

# Checkbox Component

Some **markdown** description, or whatever you want. ``` This is a what a code block looks like ```

### Basic
(preview mode)
<Preview>
<Story name='Basic'>{{
  component: CheckboxComponent,
  props: { 
      checked: true,
      label: 'Checked checkbox'
  },
}}</Story>
</Preview>

Code block example
<Source
  language='ts'
  code={dedent`
     <lib-uv-ui-checkbox label="Checked checkbox"></lib-uv-ui-checkbox>
  `}
/>

### Disabled

<Story name='Disabled'>{{
  component: CheckboxComponent,
  props: {
      disabled: true,
      label: 'Disabled checkbox'
  },
}}</Story>

<Source
  language='ts'
  code={dedent`
     <lib-uv-ui-checkbox label="Disabled checkbox" [disabled]=true></lib-uv-ui-checkbox>
  `}
/>

## ArgsTable

<ArgsTable of={CheckboxComponent} />
