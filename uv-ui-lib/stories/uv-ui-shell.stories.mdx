import { Meta, Story, ArgsTable, Source, Typeset, Preview, Canvas } from '@storybook/addon-docs/blocks';
import { moduleMetadata } from '@storybook/angular';
import { APP_BASE_HREF, CommonModule } from '@angular/common';
import { RouterModule } from '@angular/router';
import { SvgButtonModule, ICON_SET, NavbarComponent, HeaderComponent, RegisterIconModule, HeaderModule, NavbarModule } from 'projects/uv-ui-lib/src/public-api';
import dedent from 'ts-dedent';

<Meta
    title='Layouts/UI Shell'
    decorators={[
        moduleMetadata({
            imports: [HeaderModule, SvgButtonModule, NavbarModule, RegisterIconModule.forRoot(ICON_SET), RouterModule.forRoot([], { useHash: true }), RegisterIconModule.forRoot(ICON_SET)],
            providers: [{provide: APP_BASE_HREF, useValue: '/'}],
        }),
    ]}
/>

# UV UI Shell

- This shell should go in the host application and act as a shell for all microfrontends to live inside.
- In order to use the icons, you must use the uvUiLibRegisterIcon provider.
- This shell will be fixed and stretch to the size of any screen.

<Canvas>
    <Story height="100vh" width="100vw" name="UI Shell">
  {{
    template: 
    `
    <div uvUiLibRegisterIcon>
        <div class='uv-ui-shell'>
            <div class='uv-ui-shell-header'>
                <lib-uv-ui-header headerText='Title' icon='apps-quad'></lib-uv-ui-header>
            </div>
            <div class='uv-ui-shell-navbar'>
                <lib-uv-ui-navbar></lib-uv-ui-navbar>
            </div>
            <div class='uv-ui-shell-content'>
                Router Outlet/ Microfrontends go here
            </div>
        </div>
    </div>
    `
  }}
</Story>
</Canvas>

<Source
  language='html'
  code={dedent`
     <div uvUiLibRegisterIcon>
        <div class='uv-ui-shell'>
            <div class='uv-ui-shell-header'>
                <lib-uv-ui-header headerText='Title' icon='apps-quad'></lib-uv-ui-header>
            </div>
            <div class='uv-ui-shell-navbar'>
                <lib-uv-ui-navbar></lib-uv-ui-navbar>
            </div>
            <div class='uv-ui-shell-content'>
                Router Outlet/ Microfrontends go here
            </div>
        </div>
    </div>
  `}
/>

